import chatbox from './chatbox'
import model_config from './model_config'
import providers from './providers'

export { providers as providers_locales, model_config as model_config_locales, chatbox as chatbox_locales }
